# Local development configuration only.
# Copy this file to .env and replace the placeholder JWT value before testing login.
DB_HOST=127.0.0.1
JWT_SECRET=replace-with-a-long-random-development-secret
